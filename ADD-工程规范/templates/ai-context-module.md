# 模块上下文 - {{模块名称}}

## 职责

- {{职责 1}}
- {{职责 2}}

**边界**：本模块不负责 {{明确排除的职责}}。

## 核心业务规则

1. {{规则 1：例如 邮箱必须唯一}}
2. {{规则 2：例如 删除采用软删除}}
3. {{规则 3}}

## 设计决策

### {{决策：例如"为什么用 Repository 模式"}}

{{原因说明}}

### 模块依赖关系

- **依赖**：{{本模块依赖的模块}}
- **被依赖**：{{依赖本模块的模块}}

## 核心类型

```
{{粘贴本模块的核心类型定义}}
```

## 约束

- **必须**：{{例如 所有输入必须验证，密码必须加密存储}}
- **禁止**：{{例如 禁止日志打印密码，禁止返回敏感字段}}

## 修改指南

### 添加新字段

1. 修改类型定义
2. 更新数据访问层
3. 更新测试
4. 更新本文件

### 修改业务规则

1. 更新本文件的"核心业务规则"
2. 修改 service 层逻辑
3. 添加/更新测试

### 修改后检查

```bash
{{测试命令}}       # 运行测试
{{类型检查命令}}   # 类型检查
```
