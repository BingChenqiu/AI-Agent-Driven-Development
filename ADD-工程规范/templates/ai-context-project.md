# 项目上下文 - {{项目名称}}

## 项目目标

- {{目标 1}}
- {{目标 2}}

## 技术栈

- **语言**：{{语言及版本}}
- **框架**：{{框架}}
- **数据库**：{{数据库}}
- **测试**：{{测试框架}}

## 核心设计决策

### {{决策 1：例如"为什么选择 TypeScript"}}

{{原因说明}}

### 架构模式

{{采用的架构模式及原因}}

## 关键约束

- **性能**：{{例如 API 响应 < 100ms}}
- **安全**：{{例如 所有用户输入必须验证，禁止日志打印敏感信息}}
- **兼容性**：{{例如 Node 22+}}

## AI 开发指南

### 修改代码前

1. 阅读相关模块的 `.ai-context.md`
2. 理解现有类型定义
3. 确认修改不破坏现有接口

### 代码生成规则

- 先写类型，再写实现
- 单文件 < 500 行，单函数 < 50 行
- 禁止 any / 类型逃逸
- 新增功能必须带测试

### 项目命令

```bash
{{安装命令}}     # 安装依赖
{{开发命令}}     # 开发模式
{{构建命令}}     # 构建
{{测试命令}}     # 测试
{{检查命令}}     # 代码检查
```

### 命名约定

- 文件：{{kebab-case / snake_case}}
- 函数：{{camelCase / snake_case}}
- 类/接口：PascalCase
- 常量：UPPER_SNAKE_CASE

## 目录结构

```
{{填写项目目录树}}
```

## 常见问题

### 如何添加新模块？

1. 在 `src/modules/` 下创建目录
2. 创建 `.ai-context.md`
3. 定义类型 → 实现逻辑 → 暴露接口 → 编写测试
4. 更新 README 模块列表
